build/
build-*/
*.o
*.obj
compile_commands.json
.cache/

# Vendored binary, fetched by scripts/fetch-pdfium.sh — not committed.
/third_party/pdfium/

# AppImage packaging tools, fetched by scripts/build-appimage.sh — not committed.
/third_party/appimage-tools/
*.AppImage

# flatpak-builder working state/cache.
.flatpak-builder/
*.flatpak
